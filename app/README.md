# App Module

This directory contains the main application logic, including the FastAPI application entry point, Telegram bot handlers, and route definitions.

## Files

- `main.py`: FastAPI application setup.
- `bot.py`: Telegram bot initialization and command handlers.
- `routes.py`: API endpoints for authentication and webhooks.
- `ocr.py`: Mock OCR processing logic.
